services:
  app:
    build:
      context: .
      target: ${COMPOSE_TARGET:-production}
    ports:
      - ${PORT:-3000}:${PORT:-3000}
    volumes:
      - data:/data
    environment:
      DATA_PATH: /data
      JWT_SECRET:
    restart: on-failure

volumes:
  data:
