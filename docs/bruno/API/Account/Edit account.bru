meta {
  name: Edit account
  type: http
  seq: 2
}

patch {
  url: {{baseURL}}/api/account
  body: json
  auth: inherit
}

body:json {
  {
    "username": "deydoux",
    "oldPassword": "{{password}}",
    "password": "{{password}}"
  }
}
